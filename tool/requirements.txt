# holoDelta 啟動器依賴

# 基本依賴 (Python標準庫已包含)
# tkinter - GUI界面 (Python內建)
# subprocess - 進程管理 (Python內建)
# threading - 多線程 (Python內建)
# socket - 網路通訊 (Python內建)
# pathlib - 路徑處理 (Python內建)
# webbrowser - 瀏覽器控制 (Python內建)
# urllib - URL處理 (Python內建)

# 字體轉換依賴
fonttools>=4.0.0  # 字體格式轉換 (woff to ttf)

# 伺服器依賴 (需要與ServerStuff/requirements.txt同步)
fastapi>=0.68.0
uvicorn>=0.15.0

# 可選依賴 (如果需要額外功能)
psutil>=5.8.0  # 進程管理增強
requests>=2.25.0  # HTTP請求 (如果需要檢查伺服器狀態)
